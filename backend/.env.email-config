# Email Configuration
# ===================

# SMTP Server (same for both accounts)
SMTP_HOST=server215.web-hosting.com
SMTP_PORT=465

# Newsletter Email (noreply@cannoncapitalpartners.org)
SMTP_USER_NEWSLETTER=noreply@cannoncapitalpartners.org
SMTP_PASSWORD_NEWSLETTER=Sun7Blue!

# Contact Form Email (inquiries@cannoncapitalpartners.org)
SMTP_USER_CONTACT=inquiries@cannoncapitalpartners.org
SMTP_PASSWORD_CONTACT=YOUR_INQUIRIES_PASSWORD_HERE

# Fallback (if specific passwords not set)
SMTP_PASSWORD=Sun7Blue!

# Note: Add these to your main .env file
